import ReactDOM from "react-dom";
import App from "./App";

// Only used to send the user to app, but can be used to add the importent imports early and wrapp the App if needed.

ReactDOM.render(<App />, document.getElementById("root"));
